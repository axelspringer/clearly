import { Type } from '@angular/core';

import { UiDialog } from './dialog';

export const DIALOG_PROVIDERS: Array<Type<any>> = [
  UiDialog,
];

export const DIALOG_COMPONENTS: Array<Type<any>> = [
];

export { UiDialog };
