import { Type } from '@angular/core';

import { PopoverComponent } from './popover';
import { PopoverItemComponent } from './popit';

export const POPOVER_DIRECTIVES: Array<Type<any>> = [
  PopoverComponent,
  PopoverItemComponent,
];
